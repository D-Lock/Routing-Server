version: '2'

services:
  routing-server:
    build: .
    volumes:
      - "./app:/usr/src/app"
      - "./app/downloads:/usr/src/app/downloads"
    ports:
      - "1337:1337"
    links:
      - "db:redis"

  db:
    image: redis:latest
    command: redis-server
    volumes:
      - ./data:/data